{
  "name": "novelec",
  "version": "0.9",
  "description": "Site wen novelec - Lilian Monchalin",
  "author": "Isaya",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate && type nul > dist/.nojekyll",
    "preview": "nuxt preview",
    "deploy": "git subtree push --prefix dist main main"
  },
  "devDependencies": {
    "nuxt": "3.0.0-rc.1",
    "sass": "^1.52.1"
  },
  "dependencies": {
    "@fortawesome/fontawesome-svg-core": "^6.1.1",
    "@fortawesome/free-solid-svg-icons": "^6.1.1",
    "@fortawesome/vue-fontawesome": "^3.0.0-5",
    "bulma": "^0.9.4",
    "recaptcha-v3": "^1.10.0"
  }
}